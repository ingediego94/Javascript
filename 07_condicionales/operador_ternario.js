/*
OPERADOR TERNARIO:

Es una forma compacta de escribir una condición “if...else”. Se usa comúnmente cuando solo hay una condición 
simple para evaluar y se desea devolver un valor diferente dependiendo si la condición se cumple o no.

*/

let edad = 18;

let estado = (edad >= 18) ? 'Adulto' : 'Menor';

console.log(estado);
